const debug = require('debug');
const logTypes = [ 'debug', 'info', 'error' ];

module.exports = function(prefix){
	var Log = {};
	prefix = prefix || '';
	logTypes.forEach((type) => {
		Log[type] = debug(prefix + ':' + type);
	});
	return Log;
};
