'use strict';

var debug = require('debug');
var logTypes = ['debug', 'info', 'error'];

module.exports = function (prefix) {
	var Log = {};
	prefix = prefix || '';
	logTypes.forEach(function (type) {
		Log[type] = debug(prefix + ':' + type);
	});
	return Log;
};